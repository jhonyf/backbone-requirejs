define(function(require, exports, module) {
    var Backbone = require('backbone');

    Application.Collections.Base = Backbone.Collection.extend({
    });

    return Application.Collections.Base;

  }
);
